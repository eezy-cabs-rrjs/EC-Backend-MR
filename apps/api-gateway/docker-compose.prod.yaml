version: "3.8"

networks:
  internal_net:
    external: true
  public_net:
    external: true

services:
  api-gateway:
    build:
      context: ../../
      dockerfile: apps/api-gateway/Dockerfile
      target: production
    env_file:
      - .env.prod
    ports:
      - "3000:3000"
    restart: always
    networks:
      - internal_net 
      - public_net