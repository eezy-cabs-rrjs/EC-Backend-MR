version: "3.8"

networks:
  internal_net:
    external: true
  public_net:
    external: true

services:
  notifications:
    build:
      context: ../../
      dockerfile: apps/notifications/Dockerfile
      target: production
    env_file:
      - .env.prod
    ports:
      - "50006:3000"
    restart: always
    networks:
      - internal_net 
      - public_net