version: "3.8"

networks:
  internal_net:
    external: true

services:
  users:
    build:
      context: ../../
      dockerfile: apps/users/Dockerfile
      target: production
    env_file:
      - .env.prod
    ports:
      - "50008:3000"
    restart: always
    networks:
      - internal_net
