version: "3.8"

networks:
  internal_net:
    external: true

services:
  analytics:
    build:
      context: ../../
      dockerfile: apps/analytics/Dockerfile
      target: production
    env_file:
      - .env.prod
    ports:
      - "50000:3000" 
    restart: always
    networks:
      - internal_net
