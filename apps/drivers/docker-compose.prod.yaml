version: "3.8"

networks:
  internal_net:
    external: true

services:
  drivers:
    build:
      context: ../../
      dockerfile: apps/drivers/Dockerfile
      target: production
    env_file:
      - .env.prod
    ports:
      - "50004:3000"
    restart: always
    networks:
      - internal_net
